@require '@style/const.styl'
@require '@style/mixin.styl'

+b(profile-page)
  display: flex
  flex-direction: column
  align-items: center
  +e(content)
    width: 50vw
  +e(header)
    text-align: center
  +e(subscriptions)
    text-align: center
    padding: 20px