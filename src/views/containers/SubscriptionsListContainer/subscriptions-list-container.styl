@require '@style/const.styl'
@require '@style/mixin.styl'

+b(subscriptions-list-container)
  display: flex
  flex-direction: column
  align-items: flex-start
  gap: 10px

@media  (max-width: mobile-size)
  +b(subscriptions-list-container)
    align-items: center
    flex-direction: row
    gap: 8px
    width: calc(100vw - 270px)
    max-height: 50px